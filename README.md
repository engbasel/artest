# AR Showroom - معرض الواقع المعزز للنماذج ثلاثية الأبعاد

## 📋 نظرة عامة

**AR Showroom** هو تطبيق Flutter متطور يتيح للمستخدمين تحميل وعرض النماذج ثلاثية الأبعاد الخاصة بهم في الواقع المعزز (AR). يمكن للمستخدمين تحديد نماذجهم الخاصة من التخزين المحلي، تخصيص الألوان، والتفاعل معها في بيئتهم الواقعية باستخدام تقنيات ARCore (Android) و ARKit (iOS).

## ✨ المميزات الرئيسية

### 1. **اختيار النماذج المحلية**
- اختيار نماذج 3D مباشرة من تخزين الجهاز
- الصيغ المدعومة: `.glb`, `.gltf`, `.obj`
- تبديل النماذج في الوقت الفعلي أثناء عرض AR
- حفظ النماذج المستخدمة مؤخراً للوصول السريع

### 2. **تخصيص الألوان**
- تغيير ألوان النماذج بشكل فوري
- 5 خيارات ألوان مميزة متاحة
- تحديث فوري للألوان في عرض AR
- دعم تطبيق الألوان على مواد النموذج

### 3. **تجربة الواقع المعزز (AR)**
- وضع النماذج في بيئتك الحقيقية
- عرض 360 درجة والدوران الكامل
- تكبير وتصغير باستخدام الإيماءات
- سحب وإفلات لتحريك النموذج
- إعادة تعيين موضع النموذج

### 4. **عارض النماذج ثلاثية الأبعاد**
- عرض النماذج بدون AR
- تدوير وتكبير النماذج بحرية
- معاينة سريعة قبل عرض AR

## 🎯 حالات الاستخدام

- **تصميم المنتجات**: معاينة التصاميم ثلاثية الأبعاد في البيئة الحقيقية
- **التعليم**: عرض نماذج تعليمية بطريقة تفاعلية
- **التجارة الإلكترونية**: معاينة المنتجات قبل الشراء
- **الهندسة المعمارية**: عرض نماذج المباني في الموقع الفعلي
- **الترفيه والألعاب**: تفاعل مع نماذج ثلاثية الأبعاد مخصصة

## 📱 كيفية الاستخدام

### تحميل نموذج مخصص

#### من الشاشة الرئيسية:
1. افتح التطبيق
2. اضغط على زر **"Choose 3D Model"** في بطاقة "Selected Model"
3. تصفح واختر ملف `.glb`, `.gltf`, أو `.obj` من جهازك
4. سيظهر اسم النموذج في البطاقة
5. اضغط على **"View 3D Model"** لعرضه في عارض ثلاثي الأبعاد
6. أو اضغط على **"AR Experience"** لعرضه في الواقع المعزز

#### من شاشة AR:
1. أثناء عرض AR، اضغط على **أيقونة الدوران ثلاثي الأبعاد** (أسفل اليسار)
2. اختر نموذج جديد من جهازك
3. سيتم إعادة تحميل عرض AR تلقائياً مع النموذج الجديد

### تغيير الألوان:
1. في عرض AR، مرر عبر منتقي الألوان في الأسفل
2. اضغط على أي لون لتطبيقه على النموذج
3. يتم تحديث الألوان فوراً

### إعادة تعيين الموضع:
- اضغط على **أيقونة التحديث** (أسفل اليمين) لإعادة تعيين موضع النموذج

## 🏗️ البنية التقنية

### هيكل المشروع

```
lib/
├── main.dart                          # نقطة دخول التطبيق
├── models/
│   ├── ar_node_model.dart            # نموذج بيانات عقدة AR
│   ├── car_color_model.dart          # نموذج خيارات الألوان
│   └── saved_model.dart              # نموذج النماذج المحفوظة
├── screens/
│   ├── splash_screen.dart            # شاشة البداية
│   ├── home_screen.dart              # الشاشة الرئيسية مع منتقي النماذج
│   ├── ar_screen.dart                # شاشة عرض AR
│   └── model_viewer_screen.dart      # عارض النماذج ثلاثية الأبعاد
├── viewmodels/
│   └── ar_viewmodel.dart             # إدارة الحالة + منتقي الملفات
├── widgets/
│   ├── unified_ar_view_widget.dart   # ودجت عرض AR
│   └── color_picker_widget.dart      # واجهة اختيار الألوان
└── services/
    └── model_storage_service.dart    # خدمة حفظ وإدارة النماذج
```

### التقنيات المستخدمة

#### الحزم والمكتبات:
- **ar_flutter_plugin_updated**: دعم AR لكل من Android ARCore و iOS ARKit
- **provider**: إدارة الحالة
- **file_picker**: اختيار الملفات من التخزين المحلي
- **path_provider**: الوصول إلى مسارات النظام
- **permission_handler**: إدارة الأذونات
- **model_viewer_plus**: عرض النماذج ثلاثية الأبعاد
- **vector_math**: العمليات الرياضية للتحويلات ثلاثية الأبعاد

#### معمارية التطبيق:
- **MVVM Pattern**: فصل واضح بين واجهة المستخدم ومنطق الأعمال
- **Provider State Management**: إدارة حالة تفاعلية وفعالة
- **Service Layer**: طبقة خدمات لإدارة تخزين النماذج

## 🔧 المتطلبات التقنية

### متطلبات النظام
- **Flutter SDK**: 3.9.2 أو أحدث
- **Dart SDK**: متوافق مع Flutter 3.9.2

### الأذونات المطلوبة

#### Android:
- `CAMERA`: لوظائف AR
- `READ_EXTERNAL_STORAGE`: للوصول إلى ملفات النماذج ثلاثية الأبعاد (Android 12 وأقدم)
- `READ_MEDIA_IMAGES`, `READ_MEDIA_VIDEO`, `READ_MEDIA_AUDIO`: أذونات الوسائط (Android 13+)
- `INTERNET`: لتحميل الموارد إذا لزم الأمر

#### iOS:
- `NSCameraUsageDescription`: لوظائف AR
- `NSPhotoLibraryUsageDescription`: للوصول إلى الملفات

### المنصات المدعومة
- ✅ **Android**: يتطلب ARCore Support (Android 7.0 / API Level 24+)
- ✅ **iOS**: يتطلب ARKit Support (iOS 11+)

### متطلبات الأجهزة
- كاميرا خلفية
- دعم ARCore (Android) أو ARKit (iOS)
- مساحة تخزين كافية للنماذج ثلاثية الأبعاد

## 🚀 دليل التثبيت والتشغيل

### 1. تثبيت المتطلبات

تأكد من تثبيت Flutter على جهازك:
```bash
flutter doctor
```

### 2. استنساخ المشروع

```bash
git clone <repository-url>
cd artest
```

### 3. تثبيت التبعيات

```bash
flutter pub get
```

### 4. تكوين Android

#### إعدادات ARCore
تم تكوين ARCore في ملف `AndroidManifest.xml`:
- الأذونات المطلوبة مضافة
- metadata لـ ARCore محدد
- متطلبات الأجهزة معرفة

#### إعدادات ProGuard
تم تعطيل minification في وضع release لتجنب مشاكل مع AR:
```gradle
buildTypes {
    release {
        minifyEnabled false
        shrinkResources false
    }
}
```

### 5. تشغيل التطبيق

#### على محاكي/جهاز متصل:
```bash
flutter run
```

#### تحديد المنصة:
```bash
# Android
flutter run -d android

# iOS
flutter run -d ios
```

### 6. بناء التطبيق للإصدار

#### Android (APK):
```bash
flutter build apk --release
```

#### Android (App Bundle):
```bash
flutter build appbundle --release
```

#### iOS:
```bash
flutter build ios --release
```

## 🎨 واجهة المستخدم

### الشاشات الرئيسية

#### 1. شاشة البداية (Splash Screen)
- عرض شعار التطبيق
- تحميل الموارد الأولية
- انتقال سلس للشاشة الرئيسية

#### 2. الشاشة الرئيسية (Home Screen)
- **بطاقة اختيار النموذج**:
  - عرض النموذج المختار حالياً
  - زر لاختيار نموذج جديد
  - زر لإزالة النموذج المخصص

- **قسم النماذج المحفوظة**:
  - عرض قائمة النماذج المستخدمة مؤخراً
  - معلومات عن كل نموذج (الاسم، النوع، الحجم)
  - خيار حذف النماذج المحفوظة

- **بطاقات المميزات**: عرض قدرات التطبيق
- **زر عرض النموذج ثلاثي الأبعاد**: الانتقال لعارض 3D
- **زر تجربة AR**: الانتقال لشاشة الواقع المعزز

#### 3. شاشة عارض النماذج (Model Viewer Screen)
- عرض تفاعلي للنموذج ثلاثي الأبعاد
- تدوير وتكبير بالإيماءات
- معاينة سريعة قبل AR

#### 4. شاشة AR (AR Screen)
- **شريط علوي**: زر رجوع وعنوان
- **عرض AR**: كاميرا حية مع النموذج ثلاثي الأبعاد
- **منتقي الألوان**: شريط ألوان في الأسفل
- **أزرار التحكم**:
  - منتقي النماذج (أسفل اليسار)
  - إعادة تعيين الموضع (أسفل اليمين)
- **تعليمات**: نصائح للتفاعل مع AR

### الثيم والتصميم
- **Material Design 3**: تصميم حديث ومتسق
- **Dark Theme**: ثيم داكن للعين المريحة
- **Gradient Backgrounds**: خلفيات متدرجة جذابة
- **Color Scheme**:
  - اللون الأساسي: `#53a8e2` (أزرق فاتح)
  - الخلفية: `#1a1a2e`, `#16213e`, `#0f3460` (تدرجات داكنة)

## 📂 إدارة الملفات والنماذج

### اختيار الملفات
- **File Picker**: يستخدم حزمة `file_picker` للاختيار عبر المنصات
- **التحقق من الصيغة**: التحقق اليدوي من امتدادات الملفات
- **معالجة الأخطاء**: رسائل واضحة للملفات غير المدعومة

### حفظ النماذج
- **التخزين المحلي**: حفظ النماذج المستخدمة في التخزين المحلي
- **Metadata**: حفظ معلومات عن النموذج (الاسم، الحجم، المسار)
- **JSON Persistence**: استخدام SharedPreferences لحفظ قائمة النماذج

### إدارة الذاكرة
- **Lazy Loading**: تحميل النماذج عند الحاجة فقط
- **Cache Management**: إدارة ذكية للنماذج المحفوظة

## 🔍 التفاصيل التقنية المتقدمة

### معالجة AR

#### Android (ARCore):
- استخدام `ar_flutter_plugin_updated` للتكامل مع ARCore
- اكتشاف تلقائي للأسطح المستوية
- تتبع الحركة في الوقت الفعلي
- تقدير الإضاءة

#### iOS (ARKit):
- دعم كامل لـ ARKit
- تتبع دقيق للموضع
- اكتشاف الأسطح الأفقية والعمودية

### تحويلات النماذج ثلاثية الأبعاد
- **Vector Math**: استخدام `vector_math` للحسابات
- **Transformations**: دوران، تحجيم، انتقال
- **Matrix Operations**: عمليات المصفوفات للتحويلات

### إدارة الحالة

#### ARViewModel
المسؤوليات:
- إدارة حالة النموذج المختار
- معالجة اختيار الملفات
- إدارة الألوان
- التحكم في حالة AR
- حفظ واستعادة النماذج

الخصائص الرئيسية:
```dart
- selectedModelPath: String? // مسار النموذج المختار
- selectedModelName: String? // اسم النموذج
- selectedColor: CarColor // اللون المختار
- isLoading: bool // حالة التحميل
- isModelLoaded: bool // هل تم تحميل النموذج
- savedModels: List<SavedModel> // قائمة النماذج المحفوظة
```

الدوال الرئيسية:
```dart
- pickModelFile(): Future<bool> // اختيار ملف نموذج
- useDefaultModel() // استخدام النموذج الافتراضي
- changeCarColor(CarColor color) // تغيير اللون
- initializeAR() // تهيئة AR
- resetCarPosition() // إعادة تعيين الموضع
- selectSavedModel(SavedModel model) // اختيار نموذج محفوظ
- deleteSavedModel(SavedModel model) // حذف نموذج محفوظ
```

## 📝 متطلبات ملفات النماذج

### الصيغ المدعومة
- **GLB (.glb)**: صيغة glTF binary (مفضلة)
- **GLTF (.gltf)**: صيغة glTF JSON
- **OBJ (.obj)**: صيغة Wavefront OBJ

### التوصيات
- **الحجم**: أقل من 50MB للأداء الأمثل
- **Polygons**: تحسين عدد المضلعات للأجهزة المحمولة
- **Textures**: أحجام معقولة للتكسيات (2K أو أقل)
- **Materials**: مواد بسيطة لدعم أفضل لتغيير الألوان

### تحسين النماذج
- استخدام أدوات مثل Blender لتحسين النماذج
- تقليل عدد المضلعات باستخدام decimation
- ضغط التكسيات
- دمج المواد المتشابهة

## 🎯 ميزات تخصيص الألوان

### الألوان المتاحة
التطبيق يوفر 5 ألوان مميزة:
1. **أبيض** (White): `#FFFFFF`
2. **أحمر** (Red): `#FF0000`
3. **أزرق** (Blue): `#0000FF`
4. **أسود** (Black): `#000000`
5. **فضي** (Silver): `#C0C0C0`

### كيفية عمل تغيير اللون
- يتم تطبيق اللون على مواد النموذج
- بعض النماذج قد لا تدعم تغيير الألوان حسب إعداد موادها
- أفضل النتائج مع النماذج ذات المواد البسيطة ذات اللون الواحد

## 🐛 استكشاف الأخطاء

### مشاكل شائعة وحلولها

#### خطأ "Unsupported file type"
**المشكلة**: رسالة خطأ عند اختيار ملف
**الحل**:
- تأكد من أن الملف له امتداد صحيح (`.glb`, `.gltf`, `.obj`)
- الامتداد غير حساس لحالة الأحرف
- تحقق من أن الملف غير تالف

#### النموذج لا يظهر في AR
**المشكلة**: شاشة فارغة أو سوداء في وضع AR
**الحلول المحتملة**:
1. تأكد من أن ملف النموذج غير تالف
2. جرب نموذجاً مختلفاً للتحقق من وظائف AR
3. تحقق من منح أذونات الكاميرا
4. تأكد من أن جهازك يدعم AR
5. أعد تشغيل التطبيق

#### مشاكل الأداء
**الأعراض**: بطء، تقطيع، أو تجمد التطبيق
**الحلول**:
1. استخدم نماذج محسنة بعدد مضلعات أقل
2. قلل أحجام التكسيات إذا كان ممكناً
3. أغلق التطبيقات الأخرى لتحرير الذاكرة
4. أعد تشغيل الجهاز

#### الكاميرا لا تعمل
**المشكلة**: لا يمكن الوصول للكاميرا
**الحلول**:
1. تحقق من أذونات الكاميرا في إعدادات الجهاز
2. امنح الأذونات من داخل التطبيق
3. تأكد من أن تطبيقاً آخر لا يستخدم الكاميرا
4. أعد تشغيل الجهاز

#### النموذج كبير جداً
**المشكلة**: النموذج يظهر ضخماً أو صغيراً جداً
**الحل**:
- استخدم إيماءات التكبير/التصغير لضبط الحجم
- اضغط زر إعادة التعيين لإعادة الموضع الافتراضي

## 📊 اختبار التطبيق

### اختبار يدوي

#### قائمة اختبار الوظائف الأساسية:
- [ ] تحميل التطبيق بنجاح
- [ ] اختيار نموذج ثلاثي الأبعاد من الجهاز
- [ ] عرض النموذج في عارض ثلاثي الأبعاد
- [ ] الانتقال لشاشة AR
- [ ] ظهور النموذج في AR
- [ ] تغيير ألوان النموذج
- [ ] تدوير وتحريك النموذج
- [ ] إعادة تعيين موضع النموذج
- [ ] حفظ النماذج المستخدمة
- [ ] حذف النماذج المحفوظة

#### اختبار الأذونات:
- [ ] طلب أذونات الكاميرا
- [ ] طلب أذونات التخزين
- [ ] معالجة رفض الأذونات بشكل صحيح

## 🔄 السجل التحديثات

### النسخة 1.0.0 (الحالية)
**التاريخ**: ديسمبر 2025

**المميزات المضافة**:
- ✅ منتقي ملفات النماذج ثلاثية الأبعاد المحلية
- ✅ دعم صيغ `.glb`, `.gltf`, و `.obj`
- ✅ تبديل النموذج في الوقت الفعلي في AR
- ✅ معالجة محسنة للأخطاء وملاحظات المستخدم
- ✅ واجهة محسنة مع بطاقة اختيار النموذج
- ✅ أذونات التخزين لنظام Android
- ✅ نظام حفظ النماذج المستخدمة
- ✅ عارض نماذج ثلاثية الأبعاد بدون AR
- ✅ شاشة بداية (Splash Screen)

**التحسينات**:
- تحسين أداء تحميل النماذج
- واجهة مستخدم أكثر سلاسة
- معالجة أفضل للأخطاء
- تجربة مستخدم محسنة

## 🛠️ التطوير والمساهمة

### إعداد بيئة التطوير

1. **تثبيت Flutter**:
   ```bash
   # راجع https://flutter.dev/docs/get-started/install
   ```

2. **استنساخ المشروع**:
   ```bash
   git clone <repository-url>
   cd artest
   ```

3. **تثبيت التبعيات**:
   ```bash
   flutter pub get
   ```

4. **تشغيل في وضع التطوير**:
   ```bash
   flutter run
   ```

### البنية الكودية

#### قواعد البرمجة:
- استخدام Dart style guide
- تعليقات واضحة للكود المعقد
- تسمية واضحة للمتغيرات والدوال
- فصل المسؤوليات (Separation of Concerns)

#### معمارية MVVM:
```
View (Screens/Widgets)
    ↕
ViewModel (Business Logic)
    ↕
Model (Data Classes)
    ↕
Service (External Services)
```

## 📚 الموارد والمراجع

### التوثيق الرسمي:
- [Flutter Documentation](https://flutter.dev/docs)
- [ARCore Documentation](https://developers.google.com/ar)
- [ARKit Documentation](https://developer.apple.com/augmented-reality/)

### الحزم المستخدمة:
- [ar_flutter_plugin_updated](https://pub.dev/packages/ar_flutter_plugin_updated)
- [provider](https://pub.dev/packages/provider)
- [file_picker](https://pub.dev/packages/file_picker)
- [model_viewer_plus](https://pub.dev/packages/model_viewer_plus)

### أدوات مفيدة:
- **Blender**: لإنشاء وتحرير النماذج ثلاثية الأبعاد
- **Sketchfab**: لتحميل نماذج ثلاثية الأبعاد مجانية
- **Poly Haven**: موارد مجانية للنماذج والتكسيات

## 🔐 الأمان والخصوصية

### الأذونات:
- التطبيق يطلب فقط الأذونات الضرورية
- الكاميرا: لوظائف AR فقط
- التخزين: للوصول للنماذج المحددة من قبل المستخدم فقط

### البيانات:
- لا يتم إرسال أي بيانات إلى خوادم خارجية
- جميع النماذج والبيانات محفوظة محلياً
- لا يتم جمع معلومات شخصية

## 📄 الترخيص

هذا المشروع لأغراض تعليمية وتوضيحية.

## 👨‍💻 التطوير

تم البناء باستخدام:
- **Flutter 3.9.2**
- **Dart SDK**
- **AR Flutter Plugin (Updated)**
- **Material Design 3**

---

## 📞 الدعم

للأسئلة أو المشاكل:
1. راجع قسم استكشاف الأخطاء
2. تحقق من التوثيق الرسمي للحزم المستخدمة
3. افتح issue في المستودع

---

**تم التطوير بـ ❤️ باستخدام Flutter**
